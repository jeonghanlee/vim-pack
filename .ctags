# -----------------------------------------------------------
# Universal Ctags Configuration
# -----------------------------------------------------------

# Use '#' for comments instead of '"' to avoid 'non-option' warnings
# Ensure there are no leading/trailing spaces in the lines below

--recurse=yes

# Exclude unnecessary directories for better performance
--exclude=.git
--exclude=node_modules
--exclude=dist
--exclude=.vim
--exclude=*.json
--exclude=*.min.js

# --- Language Specifics ---
# universal-ctags handles Markdown automatically.
# If warnings persist, ensure universal-ctags is actually being used.
